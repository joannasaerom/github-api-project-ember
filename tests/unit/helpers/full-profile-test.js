
import { fullProfile } from 'github-project/helpers/full-profile';
import { module, test } from 'qunit';

module('Unit | Helper | full profile');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fullProfile([42]);
  assert.ok(result);
});

