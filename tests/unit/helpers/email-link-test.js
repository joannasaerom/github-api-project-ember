
import { emailLink } from 'github-project/helpers/email-link';
import { module, test } from 'qunit';

module('Unit | Helper | email link');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = emailLink([42]);
  assert.ok(result);
});

